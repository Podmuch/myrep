#include "gra.h"
int main()
{
	//HWND hwnd = GetConsoleWindow();
	//ShowWindow(hwnd, 0);
	Gra gra("172.20.18.232");
	gra.wyswietlMenu();
		//int elo = 479;
	//	char e1 = elo;
	//	char e2 = elo >> 8;
	//	int test = (unsigned char)e1 + (e2 << 8);
    return 0;
}